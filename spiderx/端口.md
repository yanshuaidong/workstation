Flask HTTP 服务

bbgnews 1123
geminihelper 1124
rtrsnews 1125


我来看看 spiderx 目录下各个子文件夹中的启动和停止脚本，以便编写统一的启动和停止脚本。

[1 tool called]

让我查看几个启动和停止脚本的内容，了解它们的工作方式：

[5 tools called]

现在我了解了各个脚本的结构。让我编写 `runall.sh` 和 `stopall.sh` 这两个统一的启动和停止脚本：

[2 tools called]

现在为这两个脚本添加执行权限：

[1 tool called]

已完成！我为你创建了两个统一管理脚本：

## 📁 `runall.sh` - 一键启动所有服务

```84:96:/Users/ysd/workstation/spiderx/runall.sh
echo "───────────────────────────────────────────────"
echo "📊 启动结果汇总:"
echo "   ✅ 成功: $SUCCESS_COUNT"
echo "   ❌ 失败/已运行: $FAIL_COUNT"
echo "   ⏭️  跳过: $SKIP_COUNT"
echo ""
echo "💡 提示:"
echo "   - 查看各服务日志: tail -f <服务目录>/nohup.out"
echo "   - 停止所有服务: ./stopall.sh"
echo "╔═══════════════════════════════════════════════╗"
echo "║        🎉 启动流程完成                        ║"
echo "╚═══════════════════════════════════════════════╝"
```

## 📁 `stopall.sh` - 一键停止所有服务

会依次停止所有服务并显示汇总结果。

## 🔧 管理的服务列表

| 服务目录 | 启动脚本 | 停止脚本 |
|---------|---------|---------|
| bbgnews | start_scheduler.sh | stop_scheduler.sh |
| clsnewscraper | start_scheduler.sh | stop_scheduler.sh |
| eastfutuscraper | start_scheduler.sh | stop_scheduler.sh |
| geminihelper | start_server.sh | stop_server.sh |
| gthtposiscraper | start_scheduler.sh | stop_scheduler.sh |

## 💡 使用方法

```bash
# 进入 spiderx 目录
cd /Users/ysd/workstation/spiderx

# 启动所有服务
./runall.sh

# 停止所有服务
./stopall.sh
```

脚本特点：
- ✅ 自动检测各服务的启动/停止脚本（`geminihelper` 使用 `start_server.sh`，其他使用 `start_scheduler.sh`）
- ✅ 显示启动/停止结果汇总
- ✅ 跳过不存在的目录或缺少脚本的服务
- ✅ 排除了 `1example` 示例目录和 `rtrsnews`、`db` 等没有启动脚本的目录